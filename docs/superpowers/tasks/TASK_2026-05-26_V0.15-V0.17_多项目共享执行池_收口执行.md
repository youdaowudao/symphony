# TASK: 多项目共享执行池 V0.15-V0.17 收口执行

## 任务边界

- 本 task 只承接这次 `V0.15 observability contract` 与 `V0.17` 宽屏首页的串行实现入口，不扩成第二套 plan。
- 执行顺序固定为：先补完 `V0.15`，通过 targeted tests 与严格 zero-context review 后，再进入 `V0.17` 宽屏首页 UI。
- 本 task 允许修改生产代码，但只允许落在对应 `V0.15` / `V0.17` 实施计划已经批准的文件范围内。
- 如果实现过程中发现必须越出 `V0.15` / `V0.17` 的既定边界，必须暂停并回到对应 spec / plan，不允许借 task 临时改 scope。

## 任务依赖

- `docs/superpowers/specs/SPEC_2026-05-26_V0.15_多项目共享执行池_最小观测gate.md`
- `docs/superpowers/plans/PLAN_2026-05-26_V0.15_多项目共享执行池_最小观测gate实施计划.md`
- `docs/superpowers/specs/SPEC_2026-05-26_V0.17_多项目共享执行池_宽屏首页UI.md`
- `docs/superpowers/plans/PLAN_2026-05-26_V0.17_多项目共享执行池_宽屏首页UI实施计划.md`
- `docs/superpowers/verification/2026-05-26_V0.17_多项目共享执行池_宽屏首页UI_contract guard验证.md`
- `docs/superpowers/tasks/TASK_2026-05-26_V0.17_多项目共享执行池_阶段7入口.md`

## 当前状态

- [x] 已完成 `V0.15` observability contract 实现：snapshot / runtime outward messages / state payload / canonical detail route / Web dashboard / terminal dashboard 都已收口到 project-aware contract。
- [x] 已完成 `V0.17` contract guard 落档，当前 authoritative 结论是“既有字段足够进入 V0.17 UI 实现”。
- [x] 已明确本轮目标不是只补文档，而是要完整完成 `V0.15 -> V0.17` 的代码、测试、复核与本地提交。
- [x] `V0.15` 的 failing tests 已写入并已转绿。
- [x] `V0.15` 生产代码已补齐。
- [x] `V0.17` 宽屏首页 UI 已实现，并已补齐异常区 / 正常 running 区 / stale 失败态 / todo pool 占位 / 项目摘要区。
- [x] review findings 已进入修复收口，文档口径已回收到“只按当前证据说话”。
- [ ] 最终 zero-context reviewer 尚未完成，不能宣称最终 review pass。
- [ ] 最终验收尚未完成，不能宣称 `V0.17` closeout 已闭环。
- [ ] 本地提交尚未完成。

## 当前阻塞

- 当前不是“只剩提交”的状态。
- 当前阻塞包括：review findings 的最终收口确认、严格 zero-context 最终复核、最终验收证据闭环、本地提交。
- 自动化测试与 code-level contract 证据已具备，但 `V0.17` UI 末端人工验收还缺 `1440x900`、`1920x1080`、溢出预算、`todo pool` 手动检查等归档证据。
- 如果最终 reviewer 发现仍存在 spec / code-quality 阻断项，必须先修复并重新复核，再允许本地提交。
- 当前新增人工验收阻断：
  - 当前首页实现与 `.superpowers/brainstorm/2782607-1779783615/content/layout-v8.html` 存在明显偏差，尤其是中文/英文比例、顶部导航数量、顶部次级 meta strip、蓝灰正式风格与 `todo pool` 承接区位置和交互。
  - 当前页面空跑不是单纯 UI 问题；本地运行日志明确反复报 `Linear API token missing in WORKFLOW.md`，因此 runtime poll 没有真实接上 Linear，页面只能显示空态或空 snapshot。
  - 当前 `elixir/project_registry.yaml` 只有一个 `project_key: "03b2b4a16461"`，与“本轮应存在两个项目配置样例”的人工口径不一致；该配置来源与缺失字段需要单独核清，不能直接假设当前 registry 正确。

## 本任务是否允许改生产代码

- 允许。
- 但实现必须遵守对应 plan 的允许修改文件范围与测试门禁。

## 下一步

- 先把当前 `layout-v8` 与现页 diff、Linear 空跑根因、registry 配置异常写入 authoritative 文档，再决定修复顺序。
- 先恢复开发阶段默认源码启动入口，避免继续误用旧 `bin/symphony` 构建产物做 UI 验收。
- 完成当前 review findings 的最终收口，并确认文档与真实证据一致。
- 完成严格 zero-context 最终复核，并把结论落到本轮收口说明中。
- 补齐 `V0.17` UI 末端人工验收与证据归档，再判断是否满足最终 closeout 条件。
- 在最终复核通过且最终验收闭环后，才允许进入本地提交。
