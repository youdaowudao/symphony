# 事故文档

这里存放事故级问题、复核结论、证据、时间线、根因和推荐动作。

当前保留一个历史目录 `新仓库迁移/`，仅作为既有材料，不作为初始化阶段的主入口。

使用规则：

1. 事故与单次 change 不混写。
2. 事故文档优先沉淀稳定事实和证据，不复制即时聊天记录。
3. 如果后续同时发生代码修复，推荐另建 `docs/changes/<change-id>/` 承接设计、计划与验证。
